# Copy to `.env` and fill in values or export in your shell.

# Azure Document Intelligence (Layout model by default)
AZURE_DI_ENDPOINT="https://<your-resource-name>.cognitiveservices.azure.com/"
AZURE_DI_KEY="<your-key>"

# Optional cache directory override (defaults to .cache/azure_di)
# AZURE_DI_CACHE_DIR=".cache/azure_di"

